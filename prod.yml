version: "3"

services:
    match:
        image: "fleet/matchsvr:latest"
        ports:
         - "80:8000"
        restart: always
        networks:
         - int
    game01:
        image: "fleet/wssvr:latest"
        ports:
         - "81:8001"
         - "91:8080"
        restart: always
    game02:
        image: "fleet/wssvr:latest"
        ports:
         - "82:8001"
         - "92:8080"
        restart: always
    game03:
        image: "fleet/wssvr:latest"
        ports:
         - "83:8001"
         - "93:8080"
        restart: always
    game04:
        image: "fleet/wssvr:latest"
        ports:
         - "84:8001"
         - "94:8080"
        restart: always
    game05:
        image: "fleet/wssvr:latest"
        ports:
         - "85:8001"
         - "95:8080"
        restart: always
    game06:
        image: "fleet/wssvr:latest"
        ports:
         - "96:8080"
        restart: always
        networks:
         - int
    game07:
        image: "fleet/wssvr:latest"
        ports:
         - "97:8080"
        restart: always
        networks:
         - int
    game08:
        image: "fleet/wssvr:latest"
        ports:
         - "98:8080"
        restart: always
        networks:
         - int
    game09:
        image: "fleet/wssvr:latest"
        ports:
         - "99:8080"
        restart: always
        networks:
         - int

networks:
    int:
        driver: overlay

